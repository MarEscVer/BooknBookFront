import { InputErrorStateMatcherExample } from './input-error-state-matcher';

describe('InputErrorStateMatcher', () => {
  it('should create an instance', () => {
    expect(new InputErrorStateMatcherExample()).toBeTruthy();
  });
});
